# Kbuild for the disable_module_sig kernel module

# Specify the module name
obj-m += sigfk.o

# Specify any source files for the module
disable_module_sig-objs := sigfk.c

# Include any additional flags or dependencies if necessary
# Here you can specify any specific options if needed
# For example:
# disable_module_sig-objs += some_other_file.o
